<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:windowBackground">

    <EditText
        android:id="@+id/etSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="15dp"
        android:background="@drawable/bg_search_text"
        android:drawableStart="@drawable/ic_search"
        android:drawablePadding="5dp"
        android:drawableTint="@color/bottom_bar_color_change"
        android:hint="@string/local_search_hint"
        android:inputType="text"
        android:maxLines="1"
        android:padding="5dp"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.divider.MaterialDivider
        android:id="@+id/mdDivider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="5dp"
        app:dividerColor="@color/purple_200"
        app:layout_constraintTop_toBottomOf="@id/etSearch" />

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tlStorage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/transparent"
        android:contentDescription="@string/local_file_tab"
        app:layout_constraintTop_toBottomOf="@id/mdDivider"
        app:tabIndicatorColor="@color/teal_200"
        app:tabIndicatorGravity="center"
        app:tabIndicatorHeight="0dp"
        app:tabSelectedTextColor="@color/purple_200"
        app:tabTextColor="@color/bottom_bar_color_change" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/vpStorageData"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tlStorage" />

</androidx.constraintlayout.widget.ConstraintLayout>